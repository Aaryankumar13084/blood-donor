
{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "echo 'No build required'",
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
